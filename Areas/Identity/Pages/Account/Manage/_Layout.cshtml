@using Microsoft.AspNetCore.Identity
@using JobCareerPlatform.Data
@inject UserManager<ApplicationUser> UserManager

@{
    var currentUser = await UserManager.GetUserAsync(User);

    if (currentUser?.UserRole == "JobSeeker")
    {
        Layout = "/Views/JobSeeker/_JobSeekerLayout.cshtml";
    }
    else if (currentUser?.UserRole == "Employer")
    {
        Layout = "/Views/Shared/_EmployerLayout.cshtml";
    }
    else if (currentUser?.UserRole == "CareerAdvisor")
    {
        Layout = "/Views/Shared/_CareerAdvisorLayout.cshtml";
    }
    else
    {
        Layout = "/Views/Shared/_AdminLayout.cshtml";
    }
}

<h1>Manage your account</h1>

<div>
    <h2>Change your account settings</h2>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <partial name="_ManageNav" />
        </div>
        <div class="col-md-9">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}
